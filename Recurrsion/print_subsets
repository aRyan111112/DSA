To store elements we make a new "List",
-for every element in original array we have two choices to take it or not take it.            very important

package Reccursion;

import java.util.ArrayList;

public class subset {

    public static void ss(int i, int[] arr, ArrayList<Integer> list) {
        if (i>=arr.length) {
            for (int j = 0; j < list.size(); j++) {
                System.out.print(list.get(j));
            }
            System.out.println();
            return;
        }

        // adding element to list
        list.add(arr[i]);
        // call function for next element but with updated list
        ss(i+1, arr, list);

        //once the base case is satisfied
        //backtrack, bring the list to it's previous form
        list.remove(list.size() - 1);
        //call function for next element but with updated list
        ss(i+1, arr, list);
    }

    public static void main(String[] args) {
        int[] arr = {3,1,2};
        ArrayList<Integer> list = new ArrayList<>();
        ss(0, arr, list);
    }
}
